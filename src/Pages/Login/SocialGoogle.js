import React from 'react';

const SocialGoogle = () => {
	return (
		<div className='w-full'>
			<button class="btn btn-error btn-outline w-full">Continue with Google</button>
		</div>
	);
};

export default SocialGoogle;